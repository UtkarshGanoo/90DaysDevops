Week 2 
linux all commmand
in the linux all is directory or file .system related binary files are stored in sbin folder.all the system configuration file are inside /etc folder  
if you wanted to find the comands are inside bin folder . 

`pwd`
Shows the current working directory.

`ls`
Lists files and directories.

`man cmd_name`
ex:-man rm
if you want to know the details about cmd first go to root user by`cd ~`and then `ls bin ` after that

`ls -l`
Lists files with detailed information.

`ls -a`
Shows all files, including hidden files.

`cd directory_name`
Changes the current directory.

`cd ..`
Moves one directory back.

`touch filename`
Creates a new empty file.

`mkdir directory_name`
Creates a new directory.

`cp source destination`
Copies a file from one location to another.

`cp -r source_directory destination`
Copies a directory and all its contents.

`mv source destination`
Moves or renames a file or directory.

`rm filename`
Removes a file.

`rm -r directory`
Removes a directory and all its contents.

`rm -rf directory`
Forcefully removes a directory and everything inside it.

`rmdir directory`
Removes an empty directory.

`cat filename`
Displays the contents of a file.

`echo "text" > filename`
Writes text into a file (overwrites existing content).

`echo "text" >> filename`
Appends text to the end of a file.

`ln source hardlink`
Creates a hard link.

`ln -s source softlink`
Creates a symbolic (soft) link.

`df`
Displays disk space usage.

`df -h`
Displays disk space in human-readable format (KB, MB, GB).

`free`
Displays RAM and swap memory usage.

`free -h`
Displays memory usage in human-readable format.

 `vmstat`
Displays virtual memory and system performance statistics.

 `vmstat -a`
Displays active and inactive memory statistics.

 `ps -ef`
Displays all running processes with detailed information.

 `kill PID`
Gracefully terminates a running process.

 `kill -9 PID`
Forcefully terminates a running process (SIGKILL).

 `ssh -i "key.pem" username@public-ip`
Connects securely to a remote Linux server using SSH.

FILE PERMISSION 
 `chmod 400 key.pem`
Changes the key file permission to read-only for the owner.

USER COMMANDS
 `sudo adduser username`
Creates a new user.

 `sudo passwd username`
Sets or changes the password of a user.

 `su username`
Switches to another user.

 `su - username`
Switches to another user and loads their login environment.

 `exit`
Returns to the previous user or exits the current shell.

 `whoami`
Displays the current logged-in user.

 `id username`
Displays the UID, GID, and groups of a user.

 `groups`
Displays the groups of the current user.

 `sudo groupadd groupname`
Creates a new group.

 `sudo groupdel groupname`
Deletes a group.

 `sudo usermod -aG groupname username`
Adds a user to a secondary group.

`sudo gpasswd -d username groupname`
Removes a user from a group.

 `sudo userdel username`
Deletes a user account.

 `sudo userdel -r username`
Deletes a user account along with its home directory.

sudo apt -get packagename
Downloads files from the Internet.

`apt update`
Updates the package list (Ubuntu).

`apt upgrade`
Upgrades installed packages.

`apt install package`
Installs a package.

`apt remove package_name`
Removes a package.

`sudo apt-get upgrade`
if you want to update your system   

`sudo shutdown -h now` or `sudo shutdown now `

when you wnat to shutdonw the instance from the command line when you use this commmand for the and -h stands for halt shutdown and now is use to 
immediatly shutdown the insatnce

`scp -i ".pem_ FILE_PATH"  "FILE_NAME OR  FILE_PATH of local that want to copy on server" SERVERPATH : "FILE_PATH of server where you want file" ` 
EX:- scp -i "C:\Users\UTKARSH GANOO\Downloads\First_Ec2_Key.pem" newest.txt  ubuntu@ec2-54-171-13-158.eu-west-1.compute.amazonaws.com:/home/ubuntu/loc_sys
if you you want to file from local system to your ec2 server  use scp (secure copy protocol).

`scp -i ".pem_ FILE_PATH" SERVERPATH : FILE_PATH where file on server  "FILE_PATH of local where you want file from server " `
EX:- scp -i "C:\Users\UTKARSH GANOO\Downloads\First_Ec2_Key.pem" ubuntu@ec2-54-171-13-158.eu-west-1.compute.amazonaws.com:/home/ubuntu/alpha.txt  "C:\my gov doc"
if you you want to file from your ec2 server TO local system use scp (secure copy protocol).

`ping ipaddress or website`
to check the connectivity btw two devices and transfer data packets

`netstat`
if you want to see the server active n/w connection and ports 

`ifconfig`
want to check show the network interfaces 

`traceroute website_name`
if you want to see the n/w hops btw source to destination 

`tracepath website_name`
if you want to to see the latency of n/w from source to destination 

`mtr website_name`
if you do ping and traceroute both 

`nslookup website_name `
to find associated ipaddress with dns  

`telnet website_name portn_no`
to connect remote server on specific port number if port is open its automatically connect 

`ip address show`
if you want to see your ip address.

`dig website_name`
if want to find the details about aomain name ipaddress  

`whois website_name`
details about website like its ragistrar,registaration expirationdate etc  

`curl -X GET api_endpoint_add` | jq
want to call api endpoints Get means want to get data from api want to butify the data we use in the last jq

`wget file_download_link`
want to download file from internet with link 

`watch cmd` `watch -n 5 cmd` 
want to run command and see the output in each 1-2 min the time can be changable run cmd in loop 

`nmap -v website_name`
if you want to idntify open ports and sacnning website 

`awk 'IN this you write code' file_name`
ex:-
 for print--> awk '{print}' mylogfil.log
 for column print -->awk '{print $1,$2}' mylogfil.log
 for filter print -->awk '/KHTML, like Gecko/ {print $1}' mylogfil.log
 for count print -->awk '/which you want to count like INFO/{COUNT++}END {print "msg you want to write ",count}' mylogfil.log
 for condition print -->awk '$2>="aplay on filter"/ {print $2.$3,$4}' mylogfil.log
powerful Linux text-processing tool used to search, filter, extract, and manipulate text from files or command output.

sed -n 'expression and write what do you need ' file name "-n to set boundry of string "
ex:-
 for print--> sed '' mylogfil.log
 for specific print -->sed -n '/INFO/p' mylogfil.log here "p for print"
 for replace word--> sed 's/applewebkit/banana/g' mylogfil.log here "s for substring and g for golablly"
 for find in which line word is coming --> sed -n -e '/what word you find for line/='filename "== to print line"
 to change this line to this line-->sed '1,10s/applewebkit/mango/g'file name "1st line to 10th line "
sed is same as awk but this work with structured and non both in this we use expression it search ,replace , insert , delet print line by line 

`grap whatyoufind file_name`
"-i"-> case insensitive when you write wrog text formate like lowercase or uppercase
"-c" -> for count the occurence of word 
if you want find expression ,pattern,word ,string in a file we use grep (global expression print) 

linux volume management 

`lsblk` 
itis a Linux command used to display information about all block storage devices

df -h
if you want to display the information the total, used, available disk space, and usage percentage of mounted file systems in a human-readable format

`sudo su `
to become a root user if you wanted to use lvm you need to be root user first

`lvm`
this is use to use the lvm for create logical volume ,physical volume ,volume group and etc 

`pvcreate /dev/volume_name `
to create physical volume you use this cmd and in the last you put the name of volume that you create form aws ebs and after attach with instance and you 
see this volume name by usin lsblk cmd 

`pvdisplay`
if you want to display the infomation about the physical volume 

`pvs`
if you want to display how much physical volume

`vgcreate group_name volume_name `
to create volume group you use this cmd 

`vgs`
you want to display how many volume grous are there in your instance  

`vgdisplay`
to display the information about volume groups

`lvcreate -L sizeof_lv -n  lv_name volumegroup_name `
ex:-`lvcreate -L 18.99G -n  mylv myvggroup `
to create logical volume from the volume group you use this cmd 

`lvs`
you want to display how many logical volume are there in your instance  

`lvdisplay`
to display the information about logical volume group

mount volumes 
The mount command is used to attach a filesystem (disk, partition, USB drive, or Logical Volume) to a directory (mount point) so that it can be accessed.

`mkfs.ext4 /dev/volume_groupname/logical_volumegroupname`
to formate logical volume and volume group  in human readable formate 

`mount formatedlogicalvolume&volume_groupname PATH directoryPath where to mount `
ex:- mount /dev/myvggroup/mylv my_logicalmoutn/my-lvmount
to mount te logical volume and volume group with dirctory  that can be accessed

`umount directoryPath`
EX:- umount my_logicalmount/my-lvmount
to unmount the directory

`mkfs -t ext4 /dev/volume_name`
to mount the disk cmd is changed but to munt cmd is same 

`lvextend -L capacity that want to increse /dev/volume_groupname/logical_volumename`
ex:-lvextend -L +5G /dev/myvggroup/mylv
to increase the capacity of logical volume

`systemctl status service_name `
ex:-systemctl status nginx
if you want to know about the running status of system 

`sudo systemctl stop service_name `
ex:-sudo systemctl stop nginx
if you want to know about the running status of system 


if you want to work on one server to another server if you want to connect server A to server B
so private key should  on the sreverA and public key on serverB.public key are in .ssh/authorize_key

`ssh -keygen`
to generate key 

`ssh - private_keyof serverA  ubuntu@ipaddressofB`
to cnnect serverA to serverb











